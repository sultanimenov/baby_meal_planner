"""Bootstrap script to generate seed data candidates using web search and LLM.

This script uses web search and LLM to generate candidate recipes from credible sources.
The output should be reviewed by humans before adding to the seed database.
"""

# Placeholder for future implementation
# This will use LangChain + web search to generate recipe candidates
# For now, recipes are manually curated in seed/recipes.json

print("Bootstrap seed generation not yet implemented.")
print("Please use manually curated recipes in seed/recipes.json")
print("Future: This script will use web search + LLM to generate candidates")


